kind: Service
apiVersion: v1
metadata:
  name: frontend-page
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 80
      protocol: TCP
      nodePort: 35000
  selector:
    app: frontend-page