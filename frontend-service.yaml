apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: fronttest
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 3000
  selector:
    app: fronttest
    tier: frontend