apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: fronttest
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 3000
    targetPort: 80
  selector:
    app: fronttest
    tier: frontend